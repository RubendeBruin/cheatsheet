```python

from PySide2.QtWidgets import QMessageBox

QMessageBox.information(parent=self,
                                title = 'Done',
                                text = text,
                                button0=QMessageBox.Ok
                                )
                                
```
